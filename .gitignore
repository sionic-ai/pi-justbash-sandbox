# Build output
# dist/ is intentionally NOT gitignored: this package is distributed via
# git (see README.md Release flow) and pi install expects dist/index.js
# to be present in the repository so it can load the factory without a
# local build step.
# dist/
*.tsbuildinfo

# Dependencies
node_modules/
.pnp.*
.yarn/

# Coverage
coverage/
*.lcov

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
.vscode/
!.vscode/settings.json.example
.idea/
*.swp
*.swo

# Env
.env
.env.local
.env.*.local

# Bun
bun.lockb
bun.lock

# Temp / caches
.tmp/
.cache/

# pi-justbash sandbox roots (defensive — they should live in os.tmpdir())
.pi-justbash-sandbox/
